import React, { FunctionComponent } from "react";

interface DrinksProps {}

export const Drinks: FunctionComponent<DrinksProps> = ({}) => {
  return (
    <span>
      <h1>Yay Drinks! </h1>
    </span>
  );
};
