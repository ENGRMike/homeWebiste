import React, { FunctionComponent } from "react";

interface HomeProps {}

export const Home: FunctionComponent<HomeProps> = ({}) => {
  return (
    <span>
      <h1>Yay Home! </h1>
    </span>
  );
};
